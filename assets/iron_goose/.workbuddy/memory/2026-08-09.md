# 2026-08-09 工作日志

## 外星人跳跃 - 音效与UI优化 + 代码提交

### 改动内容
1. **角色切换炫酷音效**：新增 `sfxCharacterSwitch()` 函数，约3秒时长
   - Phase 1 (0~1.2s)：8音符上行琶音 C4→C6，使用角色专属波形
   - Phase 2 (1.2~2.5s)：延音下行 1047→784→523Hz
   - Phase 3 (2.5~3.0s)：5音符正弦波闪烁收尾
   - 替换了原来角色切换时的 `sfxClick()` 短音
2. **暂停按钮下移**：`PAUSE_BTN_Y` 从 32 改为 60，避免太靠上不好按
3. **Git 提交并推送**：commit `0a3f712`，包含全部改动（52个文件变更）

### 角色切换音效重构（commit `450e4fb`）
1. **停止机制**：新增 `charSwitchTimers` 数组 + `charSwitchGain` 独立增益节点
   - `stopCharSwitchSound()` 清理所有定时器 + 断开 gain 实现瞬间静音
   - `playSwitchBeep()` 路由到 charSwitchGain 而非直接到 destination
2. **四角色专属主题音效**（各约3秒）：
   - Zorg（绿）：可爱卖萌 — sine波咯咯笑+弹跳旋律+开心颤音+"Whee!"收尾
   - Bloop（蓝）：水下泡泡 — sine波流水+随机气泡pops+上冒气泡流+水波下降
   - Zix（紫）：神秘宇宙 — triangle波低频drone+小调旋律+星光闪烁+宇宙微光
   - Blaze（橙）：火焰愤怒 — sawtooth波低吼蓄力+噼啪+递增growl+爆发+余烬+怒吼渐弱
